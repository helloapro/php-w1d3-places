<!DOCTYPE html>
<html>
<head>
    <link rel='stylesheet' href='https://maxcdn.bootstrapcdn.com/bootstrap/3.3.1/css/bootstrap.min.css'>
    <link rel='stylesheet' href='css/styles.css'>
    <title>Places List</title>
</head>
<body>
    <h1>Places List</h1>
    {% if places is not empty %}
        <p>Look at all these places you've been to!</p>
        <ul>
            {% for place in places %}
                <li>{{place.getName}}</li>
                <li>{{place.getStayLength}}</li>
                <li>{{place.getVisitType}}</li>
                <li><img src={{place.getPicture}}/></li>
            {% endfor %}
        </ul>
    {% endif %}

    <form action='delete_places' method='post'>
        <button type='submit'>Clear all</button>
    </form>
</body>
</html>
